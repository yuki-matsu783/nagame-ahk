---
title: 開発フローに「調査」サイクルを追加する
type: rule
description: issue-mr-flowの全体フローに、作業計画の前段として調査計画→レビュー→調査実施→結果レビューのサイクルを追加する
tags: [issue-mr-flow, workflow, process]
keywords: [調査計画, 作業計画, flow-id, HANDOFF, レビューループ]
---

# 開発フローに「調査」サイクルを追加する（issue #43）

## Context

issue #43: 「調査計画→レビュー→調査実施→結果レビュー→作業計画→レビュー→作業実施→結果レビューの
流れにする」。現状の `.claude/skills/issue-mr-flow/SKILL.md`
（唯一の実装フロー定義）は、Planモードでの実行手順作成（flow-id 4）からいきなり「作業計画」を
作ってしまい、「まず何をどう調べるか」を計画・レビューする段階が無い。issueの要望は、既存の
「作業」サイクル（計画→レビュー→実施→結果レビュー、flow-id 4〜15相当）と対称な「調査」サイクルを
その前段に追加し、作業計画そのものが調査結果を踏まえたものになるようにすること。

ユーザーとの事前確認で以下2点を決定済み:
- 調査サイクルは作業サイクルと**同じ重さ**で回す（`plans/`へのコミット・Draft MRへのレビュー
  コメント往復を経る。チャット内合意のみの軽量版にはしない）。
- 調査計画・調査結果は、作業計画と**同じ`plans/<plan名>.md`に章立てで**含める（別ファイルに
  分けない）。

## 実施内容

### 1. `.claude/skills/issue-mr-flow/SKILL.md` の全体フロー表を再構成する

flow-id 1〜3（issue起票・取得・ブランチ/Draft MR作成）はそのまま。flow-id 4以降を、既存の
「作業」サイクル（旧4〜15: Plan作成→合意→commit→レビュー→ループ→describe→compact→実施→
commit→describe→レビュー→ループ）と**同じ形を丸ごと複製**して「調査」サイクルとして新設し、
その後ろに（内容を「調査結果をもとに」始まる形へ書き換えた）元の作業サイクルを続ける。
旧16〜23（設計反映・AIアセット改善・commit・レビュー・clean up・マージ）は作業サイクルの
結果レビュー後の工程としてそのまま維持する。

新しい全体フロー（1〜35。旧→新の対応も併記）:

| 新id | ステップ | 担当 | 旧id |
|---|---|---|---|
| 1 | issueを起票する | 人間/`issue-create` | 1 |
| 2 | issueの内容を取得する | `start` | 2 |
| 3 | featureブランチとDraft MRを作成する | `start` | 3 |
| 4 | Planモードで**調査計画**を作成する（`plans/<plan名>.md`の「調査」章へ出力・コミット。worklog作成）| エージェント | (新設) |
| 5 | 調査計画に合意する | 人間 | (新設) |
| 6 | commitスキル経由でcommit・push・レビュー依頼 | エージェント | (新設) |
| 7 | MRで調査計画をレビュー・コメント（完了連絡まで待つ）| 人間 | (新設) |
| 8 | レビュー内容取得・調査計画修正・返信（7〜8ループ）| `comments`/`reply` | (新設) |
| 9 | 調査計画をもとにMR description更新 | `describe` | (新設) |
| 10 | セッションをcompact | 人間 | (新設) |
| 11 | **調査を実施**し、結果を`plans/<plan名>.md`の「調査」章・worklogに記録 | エージェント | (新設) |
| 12 | commit・push・レビュー依頼 | エージェント | (新設) |
| 13 | 調査結果をもとにMR description更新 | `describe` | (新設) |
| 14 | MRで調査結果をレビュー・コメント（完了連絡まで待つ）| 人間 | (新設) |
| 15 | レビュー内容取得・調査結果修正・返信（11〜15ループ）| `comments`/`reply` | (新設) |
| 16 | **調査結果をもとに**Planモードで**作業計画**を作成する（`plans/<plan名>.md`の「作業計画」章へ追記・コミット）| エージェント | 4 |
| 17 | 作業計画に合意する | 人間 | 5 |
| 18 | commit・push・レビュー依頼 | エージェント | 6 |
| 19 | MRで作業計画をレビュー・コメント（完了連絡まで待つ）| 人間 | 7 |
| 20 | レビュー内容取得・作業計画修正・返信（19〜20ループ）| `comments`/`reply` | 8 |
| 21 | 作業計画をもとにMR description更新 | `describe` | 9 |
| 22 | セッションをcompact | 人間 | 10 |
| 23 | 作業計画をもとに作業を進める、worklogに記録 | エージェント | 11 |
| 24 | commit・push・レビュー依頼 | エージェント | 12 |
| 25 | 作業内容をもとにMR description更新 | `describe` | 13 |
| 26 | MRでレビュー・コメント（完了連絡まで待つ）| 人間 | 14 |
| 27 | レビュー内容取得・実装/ドキュメント修正・返信（23〜27ループ）| `comments`/`reply` | 15 |
| 28 | 設計反映: `plans/`/`worklog/`→`docs/spec/`/`docs/ddr/` | エージェント | 16 |
| 29 | AIアセット改善 | エージェント | 17 |
| 30 | commit・push・レビュー依頼 | エージェント | 18 |
| 31 | MRでレビュー・コメント（完了連絡まで待つ）| 人間 | 19 |
| 32 | レビュー内容取得・設計反映/AIアセット修正・返信（28〜32ループ）| `comments`/`reply` | 20 |
| 33 | `plans/`/`worklog/`削除、`HANDOFF.md`リセット | エージェント | 21 |
| 34 | commit・push・Draft解除 | エージェント | 22 |
| 35 | マージする | 人間 | 23 |

あわせて同ファイル内の以下箇所も更新する:
- `start`サブコマンド手順3: 「全体フロー4（Planモードでの実行手順作成）」→
  「全体フロー4（Planモードでの調査計画作成）」に案内文言を修正。
- `resume`サブコマンド手順3: 「全体フロー23ステップ」→「全体フロー35ステップ」。
- 見出し「PRがflow-id 21実施前にマージされてしまった場合の対処」と本文中の`flow-id 21`（3箇所）→
  `flow-id 33`に置換。
- 見出し「レビュー完了合図の確認（全体フロー8・15・20）」→ 新しい全ループ終端を列挙し
  「（全体フロー8・15・20・27・32）」に更新（本文の完了確認ルール自体は全ループ共通で変更なし）。
- `comments`/`reply`/`describe`サブコマンドの見出し括弧（`全体フロー8・15`, `全体フロー9・13`）は
  数字が変わらないため変更不要（元々全事例を列挙しない例示であり、この扱いを踏襲）。

### 2. `HANDOFF.md` のフロー進捗状況テーブルを同じ35行に更新する

`.claude/skills/issue-mr-flow/SKILL.md`の表と常に一致させる運用（`docs-workflow.md`）のため、
`HANDOFF.md`側のテーブルも同じ内容（担当列含む）に差し替える。現在は次タスク向けにリセット済み
（进捗欄はすべて`[]`）の状態なので、行数を35に増やすだけで進捗値は変更不要。

### 3. `.claude/rules/docs-workflow.md` のループ表記ルールを更新する

「フロー進捗状況」表の運用ルール文中:
- 「23ステップに対応」→「35ステップに対応」。
- ループ範囲の例示「7〜8, 11〜15, 16〜20」→「7〜8, 11〜15, 19〜20, 23〜27, 28〜32」
  （新表の全5ループを列挙）。
- 「14と15は常に同数」の例、「1, 2, 3, 6, 9等」の単発ステップ例は、新表でも該当箇所の性質が
  変わらない（14/15は調査結果レビューループのまま、6/9も単発のcommit/describeのまま）ため
  変更不要。

### 4. `.claude/rules/git-workflow.md` と `.claude/skills/commit/SKILL.md` のcommitポイント参照を更新する

新表でcommitスキルを使う箇所はflow-id 6, 12, 18, 24, 30, 34の6箇所（旧: 6/12/18/22の4箇所）。
- `git-workflow.md`: 「flow-id 6/12/18/22」→「flow-id 6/12/18/24/30/34」
- `commit/SKILL.md`: frontmatterの`description`内と本文中の「flow-id 6/12/18/22」→
  「flow-id 6/12/18/24/30/34」（2箇所）

### 5. `dev-tools/docs/spec/issue-mr-workflow.md`（spec、最新状態を上書きする運用）を更新する

- 「全体フロー23ステップ」→「全体フロー35ステップ」（1箇所）。
- 「マージ後の取り残しクリーンアップ」節内の`flow-id 21`（2箇所）→`flow-id 33`。
- 末尾の「影響範囲」に、今回の変更を追記する形で新しいブロックを追加する
  （既存ブロックは過去の変更履歴として上書きしない。issue #43対応分として新規（追加分）ブロックを
  追記する。既存の`flow-id 17`等の過去ブロックへの言及はそのままにする＝当時の記述として正しい
  歴史的記録のため書き換えない）。

### 6. 対象外（変更しない）ファイル

- `dev-tools/docs/ddr/0009-*.md`, `0011-*.md`, `0012-*.md`: DDRは一度マージしたら追記のみ
  （不変）。過去のflow-id言及はそのまま。
- `.claude/skills/issue-create/SKILL.md`, `dev-tools/docs/spec/issue-mr-workflow.md`内の
  「flow-id 1」「flow-id 2〜3」への言及: 新表でもissue起票・ブランチ作成は引き続きflow-id
  1・2〜3のままのため変更不要。
- `dev-tools/src/archive-reentrant-plan.sh`のコメント中「flow-id 6/12」: 新表でも6・12は
  引き続きcommitポイントであり例示として正しいため変更不要。
- `.claude/agents/issue-mr-resume.md`: 具体的なflow-id番号への言及が無いため変更不要。

## 検証方法

- 変更後、`grep -rn "flow-id" .` で全参照箇所を再列挙し、上記で洗い出した箇所以外に
  取りこぼしが無いか（＝新表と矛盾する古い数字が残っていないか）を目視確認する。
- `.claude/skills/issue-mr-flow/SKILL.md`と`HANDOFF.md`の全体フロー表が完全に一致している
  （行数・担当列含め）ことを目視確認する。
- markdownのため自動テストは無し。本タスク自体は今後のissue対応から新フローが適用されるため、
  次回issue着手時に実際にこの35ステップ表に沿って進行できるかどうかで実地検証する。
